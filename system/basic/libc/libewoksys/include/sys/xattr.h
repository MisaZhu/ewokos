#ifndef SYS_XATTR_H
#define SYS_XATTR_H

/* EwokOS provides no extended-attribute support.  This stub header exists only
 * so that portable sources which #include <sys/xattr.h> still compile; no
 * xattr symbols are referenced or provided. */

#endif
