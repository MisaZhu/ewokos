cores = 0 # 0 for cpu max cores
timer_freq = 1024 #kernel timer  FPS, 0 for default freq
uart_baud = 19200

max_proc_num = 128
max_task_per_proc = 64

allocable_base_reserv_size = 0
allocable_top_reserv_size  = 0