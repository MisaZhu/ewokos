#ifndef VERIFY_CAP_SYSTEM_H
#define VERIFY_CAP_SYSTEM_H
/* cap.c includes <kernel/system.h> but uses no symbol from it that matters to
   the capability logic; this empty stub satisfies the include. */
#endif
